# FairyLedger 领域模型

柴油机配件进销存。主人通过语音口述给云端代理 Fairy 记录进货/售出，数据存 SQLite 单文件库，产出为报告与表格。**规格来源：wayfinder map「Wayfinding: FairyLedger v4 进销存规格」**；`refer/` 下 v2/v3 文档仅作历史背景。

## 商品

**图号**：
每件商品的唯一身份编号，唯一查询凭据。厂家自带；缺失时按规则自编（`ZC-` 前缀递增）。
_Avoid_: 型号、型号编号

**名称**：
商品的主名（正式名称），与图号同为主数据字段。
_Avoid_: 俗称（口语中常混用，见「别名」）

**别名（俗名）**：
商品的俗称/口述用名，一个商品可有 0..N 个，可有可无。用于语音查询时把口语映射到商品。
_Avoid_: 俗名、曾用名、同义词

**单位**：
商品的计量单位（件/副/对）。纯文本，不建换算关系。
_Avoid_: 计量单位换算、基准单位

## 往来

**往来单位**：
与主人有交易往来的个人或组织，档案同时标记是否为客户/供应商（可两者皆是）、是否挂账。
_Avoid_: 客户、供应商、账户

**挂账**：
往来单位的赊账状态。档案打标记后交易自动累计欠款，收/付款一笔冲减总额销账；无额度上限。
_Avoid_: 应收应付、逐笔核销

**现结**：
非挂账的即时结算。售出没提客户/没提挂账时默认现结，收没收到钱由主人自己明晰。
_Avoid_: 现金结、即结

**报价参考**：
按客户+产品过滤历史成交价得出的参考价，用于录入缺单价时带出或查询意图。不是独立价目表。
_Avoid_: 专享价、客户价表（v2 已否决）

## 流水与账目

**流水**：
一笔进货/售出/退货记录，是库内唯一的业务事实来源；金额由数量×单价现算，不落列。
_Avoid_: 交易、transaction

**红冲**：
业务变化（退货/换货）时冲销原单的纠错方式，必须关联原单并保留原单。
_Avoid_: 删除、冲销（v2 术语，仅作背景）

**成本快照**：
售出流水上记录的、卖出当时该商品的加权平均成本，用于毛利回溯。
_Avoid_: 成本（易与进价混淆）、现价

**期初库存**：
系统启用时一次录入的仓库现状快照，是数量事实源之一，之后不再随流水改动。
_Avoid_: 初始库存、盘点

**当前库存**：
某商品当前数量，由「期初库存 + 流水净量」随时现算得出，不落表存储。
_Avoid_: 库存表、库存余额、实时库存

**审计日志**：
记录每次修改/删除前后整行内容的留痕，用于纠错场景的完整还原。
_Avoid_: 修改记录、日志（与系统日志区分）

## 交互

**意图**：
Fairy 从主人语音中识别出的操作类型（进货/售出/查询/修改/红冲/收款/付款/期初录入/生成报告/放弃等）。意图定义"要做什么"，字段与流程细节在交互规约中展开。
_Avoid_: 命令、指令（与 CLI 命令区分）

## 报表

**毛利**：
售出金额 − 售出数量 × 当时成本快照的合计，不受后续进货影响。
_Avoid_: 利润（广义）、差价
