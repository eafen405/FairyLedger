# 派生值不落表：当前库存与金额一律现算

T1 定稿：`transactions` 不设 `amount` 列，也没有任何"当前库存"表或列；金额一律 `qty × price` 现算，当前库存由 `opening_stock` 常量基线 + `transactions` 流水净量实时求和得出。

原因是"状态由事实推导"：只存事实（期初、流水），状态（库存、金额）随查随算，永远与事实一致，不会出现 v3 里 `amount` 落列可覆盖导致的账实打架。SQLite 单机量级（万级流水）求和毫秒级，无性能代价。
