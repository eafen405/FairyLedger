# 单位不建换算：件/副/对纯文本

T1 定稿：`products.unit` 是纯文本字段（件/副/对），不建换算表（如 1 副 = 2 件），库存、成本、毛利全部以自然单位加总，不做标准单位折算。

原因是副/对在本业务里是自然单位，换算关系是人人都懂的常识（2:1），建换算表会让录入、红冲、成本计算都多一层折算，出错面大增而收益极低。同一商品混单位录入（这次 10 件、下次 5 副）的校验归交互层（T3/T4）提醒，不进入 schema。
